[build]
builder = "nixpacks"
buildCommand = "cd backend && npm ci && npm run build"

[deploy]
startCommand = "cd backend && node dist/src/main"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10
